#include <stdio.h>

/**
   * main - prints the alphabet in lowercase,
    * followed by a new line
     * Return: Always 0 (Success)
      */
int main(void)
{
		int nBten;

			for (nBten = 0; nBten <= 9; nBten++)
			{
				printf("%d", nBten);
			}
			putchar('\n');
			return (0);
}
