#include"main.h"
/**
 * mul - mulltiplies the integers
 * @a: the intger to mullitple
 * @b: the intger to multiple
 *
 * Return: the mulitple of two intger
 */
int mul(int a, int b)
{
return (a * b);
}
