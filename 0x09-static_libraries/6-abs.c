#include "main.h"

/**
  * _abs - computes absolute value of a number
  *
  * @n: the integer to compute the abs value
  *
  * Return: insigned int the abs of a number
  */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
