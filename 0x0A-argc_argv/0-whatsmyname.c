#include <stdio.h>
/**
* main - return the name of the program
* @argc: argc
* @argv: argv
* Return: 0 (Success)
*/
int main(int argc, char *argv[])
{
if (argc >= 1)
{
printf("%s\n", argv[0]);
}
return (0);
}
